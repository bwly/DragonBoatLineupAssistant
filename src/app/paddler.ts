export class Paddler {
    name: string;
    weight: number;
    side: string;

    constructor(name: string, weight: number, side: string) {
        this.name = name;
        this.weight = weight;
        this.side = side;
    }
}
